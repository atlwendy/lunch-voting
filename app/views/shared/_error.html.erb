<% if not target.nil? %>
<% if target.errors.any? %>  
	<div id="error" class="errorouter alert alert-danger">  
	  <h2><%= pluralize(target.errors.count, "error") %> prohibited this record from being saved:</h2>  
	  <ul class="error">  
	  <% target.errors.full_messages.each do |msg| %>  
		    <li><%= msg %></li>
	  <% end %>  
	  </ul>  
  </div>  
<% end %> 
<% end %>

