<div class="listing">
<span class="listingh">Editing user</span>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %> |
<%= link_to 'Delete', @user, method: :delete, data: { confirm: 'Are you sure?' } %>