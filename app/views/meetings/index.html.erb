<% provide(:title, "Meeting index") %>

<div class="listing table-responsive" style="margin-bottom: 10px">
  <div class="buddiestopbar">
      <div class="leftentity">
        <div class="contentwrapper">
          <h1><%=current_user.username%> meeting schedules</h1>
          <h3>when and where is your meeting takes place</h3>
        </div>
      </div>
    </div>

<br>

<div class="listingbox">
  <ul class="listmeetings">
  <% @meetings.each do |meeting| %>
    <li class="meeting-divider">
      <h3><%= link_to meeting.date.in_time_zone.strftime("%a, %b %d, %Y"), meeting, :class=>"meetingtime" %></h3>
      <p class="timetext"><%= meeting.date.in_time_zone.strftime("%l:%M %p") %></p>
      <span style="display:inline"><%= link_to meeting.title, meeting %> 
        <p style="display:inline; margin-right:10px"><%#= link_to raw('<i class="icon-trash icon-white"></i>'), meeting, confirm: 'Are you sure?', method: :delete %>
        </p>
      </span>
      <p class="timetext"><%=meeting.description%>
     <%#= link_to 'Destroy', meeting, method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>

  <% end %>
  </ul>
</div>

<%= link_to 'New Meeting', new_meeting_path %>
<br >
</div>